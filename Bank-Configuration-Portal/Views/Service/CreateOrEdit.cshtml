@model Bank_Configuration_Portal.Models.ServiceViewModel
@using Bank_Configuration_Portal.Resources

@{
    ViewBag.Title = Model.Id == 0 ? Language.Service_Create_Title : Language.Service_Edit_Title;
    Layout = "~/Views/Shared/_Layout.cshtml";
    var formAction = Model.Id == 0 ? "Create" : "Edit";
}

<div class="d-flex justify-content-center align-items-center" style="min-height: 80vh;">
    <div class="card shadow-sm" style="max-width: 500px; width: 100%;">
        <div class="card-header text-center bg-primary text-white">
            <h3 class="mb-0">
                @(Model.Id == 0 ? Language.Service_Create_Title : Language.Service_Edit_Title)
            </h3>
        </div>

        <div class="card-body p-4">

            @if (ViewBag.ShowForceUpdate != null && (bool)ViewBag.ShowForceUpdate)
            {
                <div class="alert alert-warning text-center">
                    <p>@Language.Service_Concurrency_Error</p>

                    <form method="post" asp-action="Edit">
                        @Html.AntiForgeryToken()
                        @Html.HiddenFor(m => m.Id)
                        @Html.HiddenFor(m => m.RowVersion)
                        @Html.HiddenFor(m => m.NameEnglish)
                        @Html.HiddenFor(m => m.NameArabic)
                        @Html.HiddenFor(m => m.MaxTicketsPerDay)
                        @Html.HiddenFor(m => m.IsActive)
                        <input type="hidden" name="forceUpdate" value="true" />

                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-danger">@Language.Force_Save</button>
                            <a href="@Url.Action("Edit", "Service", new { id = Model.Id })" class="btn btn-outline-secondary">@Language.Reload_Changes</a>
                        </div>
                    </form>
                </div>
            }

            @using (Html.BeginForm(formAction, "Service", FormMethod.Post, new { @class = "needs-validation", role = "form" }))
            {
                @Html.AntiForgeryToken()
                @Html.HiddenFor(m => m.Id)
                @Html.HiddenFor(m => m.RowVersion)

                <div id="validationSummary" class="text-danger mb-3">
                    @Html.ValidationSummary(true)
                </div>

                <div class="mb-3">
                    @Html.LabelFor(m => m.NameEnglish, new { @class = "form-label" })
                    <div class="input-group">
                        <span class="input-group-text"><i class="bi bi-translate"></i></span>
                        @Html.TextBoxFor(m => m.NameEnglish, new { @class = "form-control", placeholder = Language.Service_NameEnglish_Placeholder, maxlength = "100" })
                    </div>
                    @Html.ValidationMessageFor(m => m.NameEnglish, "", new { @class = "text-danger" })
                </div>

                <div class="mb-3">
                    @Html.LabelFor(m => m.NameArabic, new { @class = "form-label" })
                    <div class="input-group">
                        <span class="input-group-text"><i class="bi bi-translate"></i></span>
                        @Html.TextBoxFor(m => m.NameArabic, new { @class = "form-control", placeholder = Language.Service_NameArabic_Placeholder, maxlength = "100" })
                    </div>
                    @Html.ValidationMessageFor(m => m.NameArabic, "", new { @class = "text-danger" })
                </div>

                <div class="mb-3">
                    @Html.LabelFor(m => m.MaxTicketsPerDay, new { @class = "form-label" })
                    @Html.TextBoxFor(m => m.MaxTicketsPerDay, new { @class = "form-control", type = "number", min = "1" })
                    @Html.ValidationMessageFor(m => m.MaxTicketsPerDay, "", new { @class = "text-danger" })
                </div>

                <div class="mb-3 form-check">
                    @Html.CheckBoxFor(m => m.IsActive, new { @class = "form-check-input" })
                    @Html.LabelFor(m => m.IsActive, new { @class = "form-check-label" })
                </div>

                <div class="d-grid gap-2 mt-4">
                    <button type="submit" class="btn btn-primary">
                        @(Model.Id == 0 ? Language.Service_Create_Button : Language.Update_Button)
                    </button>
                </div>
            }
        </div>
    </div>
</div>
